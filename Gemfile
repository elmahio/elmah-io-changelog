source "https://rubygems.org"

# Pins the exact Jekyll/kramdown/plugin versions GitHub Pages itself builds with,
# so local `bundle exec jekyll serve` behaves the same as the real deploy.
gem "github-pages", group: :jekyll_plugins
